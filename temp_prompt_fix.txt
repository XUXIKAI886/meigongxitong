// 找到这段代码(第470-532行):
    // 头像：食物替换 + 店名替换
    return `【图片融合任务】生成${config.description}...

// 替换成:
    // 头像：食物替换 + 店名替换，使用简化提示词
    return `图片融合任务：
1. 从图片2中提取主推菜品，包括碗、盘子、汤汁、青菜、辣椒、肉丝等所有食材和容器，不能漏掉任何元素
2. 在图片1中找到食物位置，将其完全删除
3. 把图片2提取的完整菜品(包含所有元素)放到图片1的食物位置，调整大小和角度使其自然融入
4. 如果图片1中有"${templateStoreName}"文字，替换为"${storeName}"，保持原字体样式
5. 保持图片1的背景、文字、装饰完全不变

关键要求：
- 必须使用图片2的实际菜品，完整保留所有细节(颜色、形态、配菜、青菜、肉丝、容器、汤汁)
- 图片2的菜品必须100%完整出现，不能只提取部分食物，不能减少食材数量
- 图片1的原有食物必须完全被图片2的菜品替换
- 不要修改背景和装饰元素`;
